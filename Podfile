# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

#flutter_application_path = '/Users/networkteam/development/tappp_panel'

#load File.join(flutter_application_path, '.ios', 'Flutter', 'podhelper.rb')

target 'MyFramework' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyFramework
# pod 'Alamofire', '5.0.0-beta.3'
 #pod 'Alamofire'
 #install_all_flutter_pods(flutter_application_path)
 pod 'Flutter', :podspec => "https://dev-tappp-panel.s3.us-east-2.amazonaws.com/build/ios/framework/Release/Flutter.podspec", :tag => '1.0.0'
 pod 'ReachabilitySwift'
 pod 'SwiftMessageBar'
end

#post_install do |installer|
#  flutter_post_install(installer) if defined?(flutter_post_install)
#end
